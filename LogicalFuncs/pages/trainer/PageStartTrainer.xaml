<Page x:Class="LogicalFuncs.pages.trainer.PageStartTrainer"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LogicalFuncs.pages.trainer"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageStartTrainer">

    <Grid>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock x:Name="rectOne" Text="■" Foreground="{StaticResource AppBlueColor}"/>
                <TextBlock x:Name="rectTwo" Text="■" Foreground="{StaticResource AppBlueColor}"/>
                <TextBlock x:Name="rectThree" Text="■" Foreground="{StaticResource AppBlueColor}"/>
            </StackPanel>
            <TextBlock Text="Ожидание ввода..."/>
        </StackPanel>
    </Grid>
</Page>
